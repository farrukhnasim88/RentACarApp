 
@model RentACarApp.Models.Booking

@{
    ViewData["Title"] = "GetBooking";
}



<h4 class="textvw2">Please select Dates and location for your Booking</h4>
<hr />
<div class="row bglogin">

    <div class="col-md-5 text-white">
        <form asp-action="GetBookingPost">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="HireDate" class="control-label"></label><label>*</label>
                <input asp-for="HireDate" class="form-control" type="date" />
                <span asp-validation-for="HireDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ReturnDate" class="control-label"></label><label>*</label>
                <input asp-for="ReturnDate" class=" form-control" />
                <span asp-validation-for="ReturnDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Location" class="control-label"></label><label>*</label>
                <select asp-for="LocationId" class="form-control" asp-items="ViewBag.LocationId">
                    <option value="">-- Select Location --</option>
                </select>
                <span asp-validation-for="LocationId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Find Cars" class="btn btn-dark btn-lg btn-block" />
            </div>
        </form>
    </div>
    <div class="col-md-7">
        <img src="~/images/getbooking.jpeg" class=" h-100 w-100 border-info img-fluid" />
    </div>
</div> 
@section Scripts
{
    <script>
        $(document).ready(function () {
            $('#HireDate').attr('min', new Date().toISOString().split('T')[0]);
            $('#HireDate').change(function () {
                $('#ReturnDate').attr('min', $('#HireDate').val());
            })
        })

    </script>
}
 